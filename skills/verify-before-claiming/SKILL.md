---
name: verify-before-claiming
description: 在声称任何"完成/测试通过/修好了/能跑/已实现"之前，或在陈述某个 API、库、函数、配置的行为之前，或在给出重要事实性结论之前，强制执行"真正运行验证→读完整输出→解耦自检→给证据→无据则说未验证"的流程。用于杜绝"没运行就报成功""幻觉 API/不存在的包""凭记忆编事实"。Use before claiming done/passing/fixed/works, before stating how code or an API behaves, or before any high-stakes factual claim. 领域无关，编码场景尤其适用。
---

# verify-before-claiming：先查证，再开口

## 何时触发
- 准备说"完成 / 通过 / 修好了 / 能跑 / 已实现 / done / fixed / works"
- 准备陈述某个 API / 库 / 函数 / 配置项的行为或签名
- 给出重要的事实、数字、版本、价格、时效性结论
- 错误代价高（给他人建议、对外发布）

> 分工：合并/上线前的**整套方案**审查走 `blindspot-scan`；本 skill 只管你将要说出口的**那一条具体声明**是否有证据。

## 铁律（不可绕过）
> **没有在本条消息里跑过验证，就不能声称成功。** Agent 自报"完成"不是证据——真实的命令输出、退出码、diff、测试结果才是。研究中 agent 报"complete"后仍有 30–40% 规格未实现。

**无证据时禁用这些词**：`should work`、`probably`、`seems to`、`Done!`、`Perfect!`、"应该没问题"、"基本好了"、"逻辑上肯定对"。

## 执行步骤

1. **先定义"验证"是什么**：这件事怎样才算被证明？（跑测试 / build / lint / 起服务复现 / 对照官方文档 / 多源交叉）。

2. **真正去跑 / 去查**。编码场景：自己执行测试、构建、复现命令，不要让用户跑。事实场景：检索 ≥1–2 个独立来源；长文档先逐字抽取相关原文再作答。

3. **读完整输出**：看返回、退出码、报错全文，定位根因；别只看"没红就过了"。

4. **解耦自检（CoVe）**：把你的结论拆成几个独立的核查问题，**逐条独立验证**，验证时不要被你自己的草稿措辞带着走。任一条找不到支撑 → 删除或标 `⚠️未证实`。

5. **API / 依赖专项**：调用前核实函数签名与用法（读源码或官方文档）；新依赖先确认在 lockfile / registry 真实存在再用；不引入代码库未使用的库。

6. **挂证据**：保留的每条结论附 `（命令+关键输出）` 或 `（来源 URL，检索于 YYYY-MM-DD）` 或 `file:line`。

7. **诚实兜底**：没跑成 / 没查到 / 不确定 → 明说"我还没验证 X / 查不到 Y"，列出剩余 gap，给出可执行交接。**升级不确定性，而不是模拟成功。**

## 输出骨架
```
【结论】（仅已验证的部分）
【证据】命令+输出 / 来源URL+日期 / file:line
【未证实】⚠️ 列出没能验证的点（如有）
【置信度】高/中/低 + 一句理由
```

## 反合理化对照（出现这些念头就是要偷懒）
| 你冒出的念头 | 正确做法 |
|---|---|
| "改动很小，不用测" | 仍然跑一遍，小改动照样翻车 |
| "逻辑上肯定对" | 逻辑 ≠ 运行结果，跑了再说 |
| "上次跑过了" | 这次的改动后必须重新跑 |
| "用户应该会自己测" | 你能跑就你跑，别甩锅 |
| "时间紧，先说完成" | 假完成比晚完成代价大得多 |

## 红线
- 找不到来源/没跑过的，绝不写成确定结论。
- TDD 场景：先写测试看到它失败，再实现到通过；**绝不为了让测试变绿而改测试**。
- 允许并鼓励说"我不知道 / 没验证"。

*依据：Anthropic Best practices for Claude Code（show evidence, don't assert）、obra/superpowers verification-before-completion（Iron Law）、Chain-of-Verification (arXiv:2309.11495)、What Breaks When LLMs Code (arXiv:2605.30777)、slopsquatting 供应链攻击 (socket.dev / mend.io)。*
