---
description: 高风险结论交付前的 fresh-context 交叉证伪；也用于 KB 抽检（L6 协作结构）
argument-hint: [待审结论，或指向结论所在文件]
---

对下述结论执行**独立交叉证伪**（不是复述确认）：

1. **隔离上下文**：派一个不带你推理过程的独立审查者（Claude Code 用 subagent；无 subagent 则开新会话）——只给它【结论原文 + 原始数据/取数命令/相关 file:line】，**不给推导过程**，防锚定。
2. **审查者的任务是推翻**，四把刀各来一遍：字段/证据语义核实（一条计数对应啥）→ 结果分布（别只看支持面）→ 存在性断言查最上游准入 → 主动找反面证据。
3. **产出三选一**：CONFIRMED（附独立证据）/ REFUTED（附反例与差异点）/ UNVERIFIABLE（明确缺什么数据或权限）。
4. **后处理**：被推翻 → 走"被推翻不许平替"（先微复盘再回源）；抽检场景 → 结论回填 question_log"答准了吗"列，错误率记入 CHANGELOG；`[可直接决策]` 被推翻 → 触发全库标签熔断。

待审结论：$ARGUMENTS
